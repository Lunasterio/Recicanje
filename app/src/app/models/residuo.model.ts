export interface Residuo {
    uid: string;
    UserId: string;
    ResName: string;
    ResType: string;
    ImageUrl: string;
    // Agrega aquí otras propiedades que esperas en el objeto Residuo
  }